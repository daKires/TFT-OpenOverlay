// Tipos de identificador. São só `string`, mas nomeados pra deixar a intenção clara.
// Componentes/itens usam o apiName do CommunityDragon (ex. 'TFT_Item_BFSword'),
// que é estável e casa direto com a fonte de dados real que plugaremos depois.

export type ComponentId = string;
export type ItemId = string;
export type ChampionId = string;
export type TraitId = string;
